# Kept evidence

## `sample_dt_sync_2023`

relevance: the strand separating honesty from accuracy directly
claim: staleness marking halved mistrust incidents with the model unchanged -- honesty moved, trust followed
quote: a twin that knows it is stale must say so on every read

## `sample_dt_factory_2022`

relevance: the observational strand -- authority tracks the audit trail
claim: scheduling authority was extended on the strength of a published override log showing over ninety-five percent supervisor agreement, and both recorded failures were silent divergences
quote: treat every silent divergence between plant and model as a severity-one defect, because the operators will

## `sample_ml_anomaly_2023`

relevance: the analytics strand -- the accurate model's invisible failure
claim: the best-precision learned detector absorbed a slow drift into its retraining baseline and missed it, while the statistical baseline flagged it
quote: Run the z-score everywhere as the incorruptible baseline nobody retrains

## `sample_dt_overview_2024`

relevance: the vocabulary that sharpens the synthesis
claim: what distinguishes a twin from a shadow is the closed actuation loop -- exactly the step organisations withhold until the audit trail exists
quote: A crude model with two-way automatic flow is a twin; a exquisite model updated by hand is not.
